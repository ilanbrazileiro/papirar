@extends('admin.layout')
@section('content')<div class="mb-4"><h1 class="page-title">Ticket #{{ $ticket->id }}</h1><div class="small-muted">{{ $ticket->subject }}</div></div><div class="card-soft p-4 mb-4">@foreach($ticket->messages as $msg)<div class="border rounded p-3 mb-2"><strong>{{ $msg->user->name ?? $msg->adminUser->name ?? 'Equipe' }}</strong><div class="small-muted">{{ $msg->created_at?->format('d/m/Y H:i') }}</div><div>{{ $msg->message }}</div></div>@endforeach</div><div class="card-soft p-4"><form method="POST" action="{{ route('admin.tickets.reply',$ticket) }}">@csrf<textarea class="form-control" name="message" rows="4" required></textarea><button class="btn btn-primary mt-3">Responder</button></form></div>@endsection
