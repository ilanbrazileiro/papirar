@extends('admin.layout')
@section('content')<div class="d-flex justify-content-between align-items-center mb-4"><h1 class="page-title">Colaboradores</h1><a class="btn btn-primary" href="{{ route('admin.collaborators.create') }}">Novo colaborador</a></div><div class="card-soft p-4"><table class="table"><thead><tr><th>Nome</th><th>E-mail</th><th>Perfil</th><th></th></tr></thead><tbody>@foreach($collaborators as $item)<tr><td>{{ $item->name }}</td><td>{{ $item->email }}</td><td>{{ $item->role }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.collaborators.edit',$item) }}">Editar</a></td></tr>@endforeach</tbody></table>{{ $collaborators->links() }}</div>@endsection
